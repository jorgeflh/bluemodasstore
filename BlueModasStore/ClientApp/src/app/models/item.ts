import { Product } from "./product";

export class Item {
  id: number;
  orderId: number;
  productId: number;
  productName: string;
  productPrice: number;
  productImage: string;
  quantity: number;
}
